# agente-solicitudes-v3
Multi-agente de soporte de reembolsos médicos con LangGraph que integra PostgreSQL para transacciones CRUD y Elasticsearch (RAG) para documentación. También hace uso de un control de acceso basado en roles (RBAC) dinámico, memoria persistente con PostgresSaver y despliegue en Cloud Run mediante variables de entorno.
